# Notebooks

`Proyecto_Profundizacion_3_Enfermedades_en_hojas_de_tomate.ipynb` contiene el trabajo exploratorio inicial. Se ejecutó en Google Colab y descarga el dataset desde Google Drive con `gdown`.

Los notebooks nuevos deben conservar una finalidad concreta y mover las funciones reutilizables a `src/`. Para la evaluación final, reserva la partición `test/`; la línea base actual usa `val/` durante su evaluación exploratoria.
